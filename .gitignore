# Secrets
secrets/*.txt
secrets/*.json

# Local env
.env

# Traefik ACME storage (contains private keys)
traefik/acme.json

# Local dev TLS certs (mkcert wildcard — contains private key)
traefik/certs/

# Traefik dynamic configs — generated at runtime by provisioning API, never committed
traefik/conf.d/client-*.yml
traefik/conf.d/local-*.yml

# Brainstorming session files
.superpowers/

Notes/
Testing/
docs/
archive/
